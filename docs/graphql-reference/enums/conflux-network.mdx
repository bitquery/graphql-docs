---
id: conflux-network
title: ConfluxNetwork
hide_table_of_contents: false
---


export const Bullet = () => <><span style={{ fontWeight: 'normal', fontSize: '.5em', color: 'var(--ifm-color-secondary-darkest)' }}>&nbsp;●&nbsp;</span></>

export const SpecifiedBy = (props) => <>Specification<a className="link" style={{ fontSize:'1.5em', paddingLeft:'4px' }} target="_blank" href={props.url} title={'Specified by ' + props.url}>⎘</a></>

export const Badge = (props) => <><span className={props.class}>{props.text}</span></>

import { useState } from 'react';

export const Details = ({ dataOpen, dataClose, children, startOpen = false }) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details {...(open ? { open: true } : {})} className="details" style={{ border:'none', boxShadow:'none', background:'var(--ifm-background-color)' }}>
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle:'none' }}
      >
      {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};








No description


```graphql
enum ConfluxNetwork {
  conflux_hydra
  conflux_oceanus
  conflux_tethys
}
```




### Values

#### [<code style={{ fontWeight: 'normal' }}>ConfluxNetwork.<b>conflux_hydra</b></code>](#)  
> 
> 
> Conflux Hydra
> 

#### [<code style={{ fontWeight: 'normal' }}>ConfluxNetwork.<b>conflux_oceanus</b></code>](#)  
> 
> 
> Conflux Oceanus
> 

#### [<code style={{ fontWeight: 'normal' }}>ConfluxNetwork.<b>conflux_tethys</b></code>](#)  
> 
> 
> Conflux Tethys
> 



### Member of

[`conflux`](/docs/graphql-reference/queries/conflux)  <Badge class="secondary" text="query"/>

