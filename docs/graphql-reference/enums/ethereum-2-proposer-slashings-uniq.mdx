---
id: ethereum-2-proposer-slashings-uniq
title: Ethereum2ProposerSlashingsUniq
hide_table_of_contents: false
---


export const Bullet = () => <><span style={{ fontWeight: 'normal', fontSize: '.5em', color: 'var(--ifm-color-secondary-darkest)' }}>&nbsp;●&nbsp;</span></>

export const SpecifiedBy = (props) => <>Specification<a className="link" style={{ fontSize:'1.5em', paddingLeft:'4px' }} target="_blank" href={props.url} title={'Specified by ' + props.url}>⎘</a></>

export const Badge = (props) => <><span className={props.class}>{props.text}</span></>

import { useState } from 'react';

export const Details = ({ dataOpen, dataClose, children, startOpen = false }) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details {...(open ? { open: true } : {})} className="details" style={{ border:'none', boxShadow:'none', background:'var(--ifm-background-color)' }}>
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle:'none' }}
      >
      {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};








No description


```graphql
enum Ethereum2ProposerSlashingsUniq {
  blocks
  block_proposers
  dates
  slashing_slots
  slashing_epochs
  slashing_proposers
}
```




### Values

#### [<code style={{ fontWeight: 'normal' }}>Ethereum2ProposerSlashingsUniq.<b>blocks</b></code>](#)  
> 
> 
> Unique blocks
> 

#### [<code style={{ fontWeight: 'normal' }}>Ethereum2ProposerSlashingsUniq.<b>block_proposers</b></code>](#)  
> 
> 
> Unique block proposers
> 

#### [<code style={{ fontWeight: 'normal' }}>Ethereum2ProposerSlashingsUniq.<b>dates</b></code>](#)  
> 
> 
> Unique date count
> 

#### [<code style={{ fontWeight: 'normal' }}>Ethereum2ProposerSlashingsUniq.<b>slashing_slots</b></code>](#)  
> 
> 
> Unique slashing slots
> 

#### [<code style={{ fontWeight: 'normal' }}>Ethereum2ProposerSlashingsUniq.<b>slashing_epochs</b></code>](#)  
> 
> 
> Unique slashing epochs
> 

#### [<code style={{ fontWeight: 'normal' }}>Ethereum2ProposerSlashingsUniq.<b>slashing_proposers</b></code>](#)  
> 
> 
> Unique slashing proposers
> 



