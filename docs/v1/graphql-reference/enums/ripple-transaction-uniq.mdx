---
id: ripple-transaction-uniq
title: RippleTransactionUniq
hide_table_of_contents: false
---


export const Bullet = () => <><span style={{ fontWeight: 'normal', fontSize: '.5em', color: 'var(--ifm-color-secondary-darkest)' }}>&nbsp;●&nbsp;</span></>

export const SpecifiedBy = (props) => <>Specification<a className="link" style={{ fontSize:'1.5em', paddingLeft:'4px' }} target="_blank" href={props.url} title={'Specified by ' + props.url}>⎘</a></>

export const Badge = (props) => <><span className={props.class}>{props.text}</span></>

import { useState } from 'react';

export const Details = ({ dataOpen, dataClose, children, startOpen = false }) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details {...(open ? { open: true } : {})} className="details" style={{ border:'none', boxShadow:'none', background:'var(--ifm-background-color)' }}>
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle:'none' }}
      >
      {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};








No description


```graphql
enum RippleTransactionUniq {
  dates
  blocks
  indices
  hashes
  senders
  types
  sourceTags
  accountTxnIds
  sequences
  lastLedgerSequences
  flags
  results
}
```




### Values

#### [<code style={{ fontWeight: 'normal' }}>RippleTransactionUniq.<b>dates</b></code>](#)  
> 
> 
> Uniq dates count
> 

#### [<code style={{ fontWeight: 'normal' }}>RippleTransactionUniq.<b>blocks</b></code>](#)  
> 
> 
> Uniq blocks count
> 

#### [<code style={{ fontWeight: 'normal' }}>RippleTransactionUniq.<b>indices</b></code>](#)  
> 
> 
> Uniq indices count
> 

#### [<code style={{ fontWeight: 'normal' }}>RippleTransactionUniq.<b>hashes</b></code>](#)  
> 
> 
> Uniq hashes count
> 

#### [<code style={{ fontWeight: 'normal' }}>RippleTransactionUniq.<b>senders</b></code>](#)  
> 
> 
> Uniq senders count
> 

#### [<code style={{ fontWeight: 'normal' }}>RippleTransactionUniq.<b>types</b></code>](#)  
> 
> 
> Uniq types count
> 

#### [<code style={{ fontWeight: 'normal' }}>RippleTransactionUniq.<b>sourceTags</b></code>](#)  
> 
> 
> Uniq source tags count
> 

#### [<code style={{ fontWeight: 'normal' }}>RippleTransactionUniq.<b>accountTxnIds</b></code>](#)  
> 
> 
> Uniq account txn id bins count
> 

#### [<code style={{ fontWeight: 'normal' }}>RippleTransactionUniq.<b>sequences</b></code>](#)  
> 
> 
> Uniq sequences count
> 

#### [<code style={{ fontWeight: 'normal' }}>RippleTransactionUniq.<b>lastLedgerSequences</b></code>](#)  
> 
> 
> Uniq last ledger sequences count
> 

#### [<code style={{ fontWeight: 'normal' }}>RippleTransactionUniq.<b>flags</b></code>](#)  
> 
> 
> Uniq flags count
> 

#### [<code style={{ fontWeight: 'normal' }}>RippleTransactionUniq.<b>results</b></code>](#)  
> 
> 
> Uniq results count
> 



